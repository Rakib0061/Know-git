// import * as bootstrap from 'bootstrap';
import { Tooltip, Toast, Popover } from 'bootstrap';

// please remove those below code it's for escaping eslint variable defind but use not use rule
const bs = Popover;
const bs2 = Tooltip;
const bs3 = Toast;

bs.toString();
bs2.toString();
bs3.toString();
